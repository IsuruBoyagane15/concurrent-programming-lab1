The project contains three files with the 3 implementations Serial program, Parallel program with one mutex and Parallel program with read-write locks.

To run the Serial program, open a cmd and enter,
	gcc -g -Wall -o linkedlist SerialProgram.c -lpthread -lm
once the program is built, enter ./s to execute the program

